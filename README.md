# Python_subroutines

A bunch of subroutines as Jupyter notebooks in Python 3, repurposed from various codes I have written for use in my research work over the years.
Each notebook will also contains sections generating test dataset(s), and a test run of the subroutine using the generated dataset(s).
